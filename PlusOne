class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        newList = " "
        newList = ''.join([str(a) for a in digits])
        newList = int(newList) + 1
        newList = str(newList)
        newList = [int(numeric_string) for numeric_string in newList]
        return newList
